diff --git a/node_modules/react-quill/lib/index.js b/node_modules/react-quill/lib/index.js
index 2735747..b5402d3 100644
--- a/node_modules/react-quill/lib/index.js
+++ b/node_modules/react-quill/lib/index.js
@@ -1,6 +1,6 @@
 'use strict';
 
-var React = require('react');
+var React = require('react'); 
 var ReactDOM = require('react-dom');
 var Quill = require('quill');
 var PropTypes = require('prop-types');
@@ -12,7 +12,7 @@ function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'defau
 var React__default = /*#__PURE__*/_interopDefaultLegacy(React);
 var ReactDOM__default = /*#__PURE__*/_interopDefaultLegacy(ReactDOM);
 var Quill__default = /*#__PURE__*/_interopDefaultLegacy(Quill);
-var PropTypes__default = /*#__PURE__*/_interopDefaultLegacy(PropTypes);
+var PropTypes__default = /*#__PURE__*/_interopDefaultLegacy(PropTypes); 
 
 function _extends() {
   _extends = Object.assign || function (target) {
@@ -351,7 +351,7 @@
     var el = this.getEditor().root;
 
     if (el) {
-      el.classList.toggle('ql-blank', this.isEditorEmpty());
+      el.classList.toggle('ql-blank', this.isEditorEmpty(this.getEditor()));
     }
   },
 
@@ -367,7 +367,7 @@
     }
   },
 
-  isEditorEmpty: function isEditorEmpty() {
+  isEditorEmpty: function isEditorEmpty(editor) {
     return this.getEditorContents().length === 0;
   },
 
@@ -389,12 +389,17 @@
     return this.state.value;
   },
 
+  // React 18 Strict Mode workaround
+  // https://github.com/zenoamaro/react-quill/issues/823#issuecomment-1189495339
   setEditorContents: function setEditorContents(value) {
+    const editor = this.getEditor();
     var sel = this.getEditorSelection();
     if (typeof value === 'string') {
-      this.getEditor().setContents(this.getEditor().clipboard.convert(value));
+      editor.setContents(editor.clipboard.convert(value));
     } else {
-      this.getEditor().setContents(value);
+      editor.setContents(value);
     }
+
+    this.onEditorChange(editor.getContents(),'api', 'api', editor);
   },
 
   setEditorSelection: function setEditorSelection(range, source) {
@@ -406,8 +411,8 @@
     }
   },
 
-  focus: function focus() {
-    ReactDOM__default.findDOMNode(this).querySelector('[contenteditable="true"]').focus();
+  focus: function focus() { 
+    this.getEditor().focus();
   },
 
   blur: function blur() {
@@ -432,7 +437,7 @@
   render: function render() {
     return React__default.createElement('div', {
       id: this.props.id,
-      style: this.props.style,
+      style: this.props.style, 
       key: this.state.generation,
       className: ['quill'].concat(this.props.className).join(' '),
       ref: this.setEditingArea,
